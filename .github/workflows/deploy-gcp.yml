# Disabled: GKE requires billing. Uncomment and configure secrets if you enable billing.
# name: Deploy to GCP
#
# on:
#   push:
#     branches: [main]
#   workflow_dispatch:
#
# permissions:
#   contents: read
#
# env:
#   GCP_REGION: us-central1
#   TF_IN_AUTOMATION: "true"
#
# jobs:
#   deploy:
#     name: Terraform Deploy (GCP)
#     runs-on: ubuntu-latest
#
#     steps:
#       - name: Checkout
#         uses: actions/checkout@v4
#
#       - name: Authenticate to Google Cloud (static key)
#         uses: google-github-actions/auth@v2
#         with:
#           credentials_json: ${{ secrets.GCP_SA_KEY }}
#
#       - name: Setup Terraform
#         uses: hashicorp/setup-terraform@v3
#         with:
#           terraform_version: "~1.9"
#
#       - name: Terraform Init
#         working-directory: infrastructure/terraform/gcp
#         run: |
#           terraform init \
#             -backend-config="bucket=${{ secrets.TF_STATE_BUCKET_GCP }}" \
#             -backend-config="prefix=terraform/gcp"
#
#       - name: Terraform Validate
#         working-directory: infrastructure/terraform/gcp
#         run: terraform validate
#
#       - name: Terraform Plan
#         working-directory: infrastructure/terraform/gcp
#         run: |
#           terraform plan \
#             -var="project_id=${{ secrets.GCP_PROJECT_ID }}" \
#             -var="region=${{ env.GCP_REGION }}" \
#             -var="artifact_registry_url=${{ secrets.ARTIFACT_REGISTRY_URL }}" \
#             -var="image_tag=${{ github.sha }}" \
#             -var="db_password=${{ secrets.DB_PASSWORD }}" \
#             -var="jwt_secret=${{ secrets.JWT_SECRET }}" \
#             -var="admin_email=${{ secrets.ADMIN_EMAIL }}" \
#             -var="admin_password=${{ secrets.ADMIN_PASSWORD }}" \
#             -out=tfplan
#
#       - name: Terraform Apply
#         working-directory: infrastructure/terraform/gcp
#         run: terraform apply -auto-approve tfplan
