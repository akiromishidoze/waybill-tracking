# API base URL (proxied through Vite in dev; set to full URL in production)
VITE_API_URL=/api

# WebSocket URL for real-time tracking updates
VITE_WS_URL=/ws

# Sentry DSN for error tracking (optional; leave blank to disable)
VITE_SENTRY_DSN=
